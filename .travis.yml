language: ruby

rvm:
  - 1.9.3
  - 2.0.0
  - 2.1.0
  - 2.2.0

gemfile:
  - Gemfile
  - gemfiles/Rails_master.gemfile

branches:
  only:
    - master

matrix:
  exclude:
    - rvm: 1.9.3
      gemfile: gemfiles/Rails_master.gemfile

  allow_failures:
    - gemfile: gemfiles/Rails_master.gemfile
